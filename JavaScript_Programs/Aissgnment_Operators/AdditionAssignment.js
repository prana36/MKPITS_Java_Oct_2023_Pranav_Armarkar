// Sums up left and right operand values and then assigns the result to the left operand.

let num1 = 2;
let num2 = 5;
console.log((num1 += num2));

// concatination  operator ( + ) is used for string concatenation
let name1 = "Ashik";
let name2 = "Kumar";
console.log((name1 += name2));
